# ⚠️ WARNING: FOR DEMO/TESTING ONLY!
# Never expose real private keys in frontend!

# Sponsor wallet private key (Base64)
# Generate with:
# const keypair = new Ed25519Keypair();
# console.log(Buffer.from(keypair.getSecretKey()).toString('base64'));
VITE_SPONSOR_PRIVATE_KEY=your_base64_private_key_here

# Fund this address with SUI from:
# https://faucet.sui.io/
